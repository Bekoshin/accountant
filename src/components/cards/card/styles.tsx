import {StyleSheet} from "react-native";

export const styles = StyleSheet.create({
  mainContainer: {
    borderRadius: 5,
    marginBottom: 6,
  },
  contentContainer: {
    flexDirection: 'row',
    backgroundColor: 'white',
    borderRadius: 4,
    padding: 10,
  },
});
